DB_USERNAME=my_username
DB_PASSWORD=my_password
DB_NAME=my_db_name
DB_DATA_DIR=/path/to/my/db/data/dir
DB_LOGFILE=/path/to/my/db/logfile

LOCAL_IMAGE_STORE=<path_to_my_local_image_store>
DOCKER_IMAGE_STORE=/data/images  # can leave as this default

# uncomment for running locally
# IMAGE_STORE_PATH=$LOCAL_IMAGE_STORE
# DATABASE_URL=postgresql://$DB_USERNAME:$DB_PASSWORD@localhost:5432/$DB_NAME

# uncomment for running via docker container
IMAGE_STORE_PATH=$DOCKER_IMAGE_STORE
DATABASE_URL=postgresql://$DB_USERNAME:$DB_PASSWORD@host.docker.internal:5432/$DB_NAME  # for running via docker
